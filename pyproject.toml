[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "uk-tax-mcp"
version = "0.1.0"
description = "Deterministic HMRC tax rule engine exposed via Model Context Protocol"
requires-python = ">=3.9"
dependencies = [
    "pydantic>=2.0",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
server = [
    "mcp[cli]>=1.0.0",
    "click>=8.0",
]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "httpx>=0.27",
    "ruff>=0.4",
    "mypy>=1.10",
    "types-PyYAML>=6.0",
    "eval-type-backport>=0.2; python_version < '3.10'",
]
http = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "prometheus-client>=0.20",
]
extractor = [
    "anthropic>=0.25",
]
indexing = [
    "azure-cosmos>=4.7",
    "azure-identity>=1.17",
    "openai>=1.30",
    "tiktoken>=0.7",
    "httpx>=0.27",
    "beautifulsoup4>=4.12",
    "ruamel.yaml>=0.18",
]

[project.scripts]
uk-tax-mcp = "hmrc_tax_mcp.server:main"

[tool.hatch.build.targets.wheel]
packages = ["src/hmrc_tax_mcp"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--tb=short"

[tool.ruff]
line-length = 100
src = ["src"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.mypy]
python_version = "3.11"
strict = true

[[tool.mypy.overrides]]
# mcp and anthropic are optional extras — stubs not always available
module = ["mcp", "mcp.*", "anthropic"]
ignore_missing_imports = true
ignore_errors = true

[[tool.mypy.overrides]]
# fastapi and pydantic are optional extras in the dev environment
module = ["fastapi", "pydantic", "pydantic.*"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = ["prometheus_client", "prometheus_client.*"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
# http_dev is a dev-only FastAPI wrapper; its optional imports are environment-dependent
module = ["hmrc_tax_mcp.http_dev"]
ignore_errors = true

[[tool.mypy.overrides]]
# api.py uses FastAPI types that are not always available in strict mode
module = ["hmrc_tax_mcp.api"]
ignore_errors = true

[[tool.mypy.overrides]]
# metrics.py uses prometheus_client which ships no stubs
module = ["hmrc_tax_mcp.metrics"]
ignore_errors = true

[[tool.mypy.overrides]]
# indexing extras — azure-cosmos, azure-identity, openai, tiktoken, httpx, bs4
module = ["azure", "azure.*", "openai", "openai.*", "tiktoken", "httpx", "bs4", "ruamel", "ruamel.*"]
ignore_missing_imports = true
